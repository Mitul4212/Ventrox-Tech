services:
  - type: web
    name: ventrox-tech
    env: node
    buildCommand: rm -f package-lock.json && npm install && npm run build
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: SESSION_SECRET
        sync: false
      - key: NODE_VERSION
        value: 20.11.0
